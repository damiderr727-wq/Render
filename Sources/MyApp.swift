import SwiftUI

// Einstiegspunkt der App. Es darf im Projekt nur EINE Datei mit @main geben -
// falls schon eine eigene existiert, diese hier NICHT zusaetzlich importieren.
@main
struct VillaNachtschattenApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
